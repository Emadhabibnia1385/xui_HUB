````md
<div align="center">

<!-- اگر نمی‌خوای فایل اضافه تو ریپو باشه، لوگو رو بیرون آپلود کن و لینک مستقیمش رو جایگزین کن -->
<!-- <img src="LINK_TO_LOGO.png" alt="xui_HUB Logo" width="240"/> -->

# 🤖 xui_HUB

### هاب مدیریت پنل‌های 3x-ui در تلگرام + ادغام کلاینت‌ها از چند پورت به یک پورت (مثل 443)

[![Python](https://img.shields.io/badge/Python-3.8+-3776AB?style=for-the-badge&logo=python&logoColor=white)](https://www.python.org)
[![Telegram](https://img.shields.io/badge/Telegram-Bot-26A5E4?style=for-the-badge&logo=telegram&logoColor=white)](https://telegram.org)
[![Stars](https://img.shields.io/github/stars/Emadhabibnia1385/xui_HUB?style=for-the-badge&logo=github)](https://github.com/Emadhabibnia1385/xui_HUB/stargazers)

**ربات تلگرام برای مدیریت پنل‌ها و ادغام خودکار کلاینت‌های چند Inbound داخل یک پورت مقصد**

[نصب سریع](#-نصب-سریع) • [ویژگی‌ها](#-ویژگیها) • [راهنمای استفاده](#-راهنمای-استفاده) • [رفع مشکل](#-رفع-مشکلات-رایج) • [پشتیبانی](#-پشتیبانی)

---

</div>

## 📖 درباره xui_HUB

**xui_HUB** یک ربات تلگرام برای مدیریت پنل‌های **3x-ui / x-ui** است که به شما اجازه می‌دهد:

- چند پنل (سرور) را داخل ربات ذخیره کنید
- از داخل تلگرام عملیات **ادغام کلاینت‌ها** از چند پورت (Inbound) به یک پورت مقصد را انجام دهید (مثلاً 443)

> ✅ این ربات **ادمین‌اونلی نیست** و هر کسی که ربات را استارت کند می‌تواند برای پنل‌های خودش از آن استفاده کند.  
> ✅ اطلاعات فقط در حد **پنل‌های هر کاربر** ذخیره می‌شود (کاربرها پنل‌های همدیگر را نمی‌بینند).  
> ✅ مراحل ادغام و داده‌های موقت بعد از اتمام عملیات **پاک می‌شوند**.

---

## ✨ ویژگی‌ها

<table>
<tr>
<td width="33%" align="center">

### 🛠 مدیریت پنل‌ها
✅ افزودن پنل  
✅ ویرایش پنل  
✅ حذف پنل  
✅ ذخیره جدا برای هر کاربر

</td>
<td width="33%" align="center">

### 🔀 ادغام پورت‌ها
✅ ادغام کلاینت‌های چند پورت ورودی  
✅ انتقال به پورت مقصد (مثل 443)  
✅ جلوگیری از کلاینت تکراری (در حد کلیدهای شناسه)

</td>
<td width="33%" align="center">

### 🔐 اتصال امن
✅ اتصال با SSH  
✅ اجرای دستورها روی سرور  
✅ بکاپ best-effort از دیتابیس قبل از تغییر

</td>
</tr>
</table>

---

## 🚀 نصب سریع

### نصب خودکار (پیشنهادی) ⚡

روی سرور (Ubuntu/Debian) فقط این دستور را اجرا کنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Emadhabibnia1385/xui_HUB/main/install.sh)
````

بعد از نصب، توکن ربات را وارد می‌کنید و سرویس به صورت خودکار اجرا می‌شود.

> **نیازمندی:** اجرا با `root` یا `sudo`

---

## 🎮 مدیریت سرویس (systemd)

```bash
# وضعیت
systemctl status xuihub --no-pager -l

# شروع
systemctl start xuihub

# توقف
systemctl stop xuihub

# ریستارت
systemctl restart xuihub

# لاگ زنده
journalctl -u xuihub -f
```

---

## 📱 راهنمای استفاده

### 1) ساخت ربات و گرفتن Token

1. به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot` را ارسال کنید
3. نام و یوزرنیم ربات را وارد کنید
4. **توکن** را کپی کنید و هنگام نصب وارد کنید

### 2) کار با ربات

* `/start` را بزنید
* از منوی اصلی:

  * **🛠 مدیریت پنل‌ها** → افزودن/ویرایش/حذف پنل
  * **🔀 ادغام پورت‌ها** → انتخاب پنل → وارد کردن پورت‌های ورودی → پورت مقصد → `OK`

---

## 🧩 افزودن پنل (چه اطلاعاتی لازم است؟)

در بخش **اضافه کردن پنل** این موارد را می‌پرسد:

* IP یا دامنه پنل
* HTTP یا HTTPS
* پورت پنل
* پچ پنل (مثلاً `/panel`)
* یوزرنیم/پسورد پنل
* SSH Host (سرور)
* SSH User / SSH Port / SSH Password

> نکته: فعلاً برای ادغام، ربات از SSH استفاده می‌کند (اطلاعات پنل صرفاً برای نگهداری/توسعه‌های بعدی است).

---

## 🔀 ادغام پورت‌ها دقیقاً چطور کار می‌کند؟

* شما پورت مقصد را **از قبل داخل پنل** می‌سازید (Inbound مقصد)
* سپس در ربات:

  1. پنل را انتخاب می‌کنید
  2. تعداد پورت‌های ورودی را می‌دهید
  3. پورت‌های ورودی را وارد می‌کنید
  4. پورت مقصد را می‌دهید
  5. `OK` می‌زنید
* ربات روی سرور:

  * دیتابیس `x-ui.db` را پیدا می‌کند
  * اگر جدول `clients` وجود داشته باشد → روش Table
  * اگر جدول `clients` وجود نداشته باشد → کلاینت‌ها را از JSON داخل `inbounds.settings` ادغام می‌کند
  * در پایان سرویس x-ui را ریستارت می‌کند (best-effort)

---

## 🐛 رفع مشکلات رایج

<details>
<summary><b>ربات اجرا نمی‌شود</b></summary>

```bash
journalctl -u xuihub -n 100 --no-pager
systemctl status xuihub --no-pager -l
```

</details>

<details>
<summary><b>خطا: ERR_NO_SQLITE3</b></summary>

روی سرور مقصد sqlite3 نصب نیست:

```bash
sudo apt-get update -y
sudo apt-get install -y sqlite3
sudo systemctl restart xuihub
```

</details>

<details>
<summary><b>خطا: دیتابیس x-ui.db پیدا نشد</b></summary>

مسیر دیتابیس در سیستم شما متفاوت است یا سطح دسترسی sudo مشکل دارد.
روی سرور بررسی کنید:

```bash
sudo find / -maxdepth 6 -name "x-ui.db" 2>/dev/null | head -n 5
```

</details>

<details>
<summary><b>خطا: ERR_NO_SETTINGS_COL</b></summary>

در نسخه شما ستون ذخیره‌سازی تنظیمات نام دیگری دارد. روی سرور این را بزنید و خروجی را ارسال کنید:

```bash
DB=$(sudo find / -maxdepth 6 -name "x-ui.db" 2>/dev/null | head -n 1)
sudo sqlite3 "$DB" "PRAGMA table_info(inbounds);"
```

</details>

---

## 🔐 نکات امنیتی

* این پروژه برای اجرای عملیات ادغام نیاز به **SSH Password** دارد.
* توصیه می‌شود:

  * یک یوزر جدا برای عملیات بسازید
  * دسترسی sudo را محدود و مشخص کنید
  * توکن ربات را محرمانه نگه دارید

---

## 📁 ساختار پروژه

طبق درخواست شما، پروژه فقط همین فایل‌ها را دارد:

```
xui_HUB/
├── README.md
├── LICENSE
├── bot.py
├── install.sh
└── requirements.txt
```

---

## 🤝 مشارکت

مشارکت شما خوشحال‌کننده است:

1. Fork
2. ساخت Branch
3. Commit
4. Pull Request

---

## 📞 پشتیبانی

<div align="center">

[![Telegram](https://img.shields.io/badge/Developer-@EmadHabibnia-blue?style=for-the-badge\&logo=telegram)](https://t.me/EmadHabibnia)

</div>

---

<div align="center">

**ساخته شده توسط Emad Habibnia**

</div>
```
